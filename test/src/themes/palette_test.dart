import 'package:bat_theme/bat_theme.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('BatPalette', () {
    test('primary should be a Color object', () {
      expect(BatPalette.primary, isA<Color>());
    });

    test('primary80 should be a Color object', () {
      expect(BatPalette.primary80, isA<Color>());
    });

    test('primary60 should be a Color object', () {
      expect(BatPalette.primary60, isA<Color>());
    });

    test('primary30 should be a Color object', () {
      expect(BatPalette.primary30, isA<Color>());
    });

    test('secondary should be a Color object', () {
      expect(BatPalette.secondary, isA<Color>());
    });

    test('secondary80 should be a Color object', () {
      expect(BatPalette.secondary80, isA<Color>());
    });

    test('secondary60 should be a Color object', () {
      expect(BatPalette.secondary60, isA<Color>());
    });

    test('secondary30 should be a Color object', () {
      expect(BatPalette.secondary30, isA<Color>());
    });

    test('secondary10 should be a Color object', () {
      expect(BatPalette.secondary10, isA<Color>());
    });

    test('white should be a Color object', () {
      expect(BatPalette.white, isA<Color>());
    });

    test('white80 should be a Color object', () {
      expect(BatPalette.white80, isA<Color>());
    });

    test('white60 should be a Color object', () {
      expect(BatPalette.white60, isA<Color>());
    });

    test('white30 should be a Color object', () {
      expect(BatPalette.white30, isA<Color>());
    });

    test('grey should be a Color object', () {
      expect(BatPalette.grey, isA<Color>());
    });

    test('grey80 should be a Color object', () {
      expect(BatPalette.grey80, isA<Color>());
    });

    test('grey60 should be a Color object', () {
      expect(BatPalette.grey60, isA<Color>());
    });

    test('grey30 should be a Color object', () {
      expect(BatPalette.grey30, isA<Color>());
    });

    test('grey10 should be a Color object', () {
      expect(BatPalette.grey10, isA<Color>());
    });

    test('success should be a Color object', () {
      expect(BatPalette.success, isA<Color>());
    });

    test('success80 should be a Color object', () {
      expect(BatPalette.success80, isA<Color>());
    });

    test('success60 should be a Color object', () {
      expect(BatPalette.success60, isA<Color>());
    });

    test('success30 should be a Color object', () {
      expect(BatPalette.success30, isA<Color>());
    });

    test('error should be a Color object', () {
      expect(BatPalette.error, isA<Color>());
    });

    test('error80 should be a Color object', () {
      expect(BatPalette.error80, isA<Color>());
    });

    test('error60 should be a Color object', () {
      expect(BatPalette.error60, isA<Color>());
    });

    test('error30 should be a Color object', () {
      expect(BatPalette.error30, isA<Color>());
    });

    test('black should be a Color object', () {
      expect(BatPalette.black, isA<Color>());
    });
  });
}
